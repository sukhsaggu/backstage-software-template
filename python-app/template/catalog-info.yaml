apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{values.app_name}}
  description: Python app that displays time, names ${{values.app_name}}
  annotations:
    github.com/project-slug: sukhsaggu/${{values.app_name}}
spec:
  type: service
  owner: development
  lifecycle: experimental